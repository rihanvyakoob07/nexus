import PortalShell from "@/components/PortalShell";

export default function EngineerLayout({ children }: { children: React.ReactNode }) {
  return <PortalShell role="engineer">{children}</PortalShell>;
}
