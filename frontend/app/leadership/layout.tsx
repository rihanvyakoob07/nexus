import PortalShell from "@/components/PortalShell";

export default function LeadershipLayout({ children }: { children: React.ReactNode }) {
  return <PortalShell role="leadership">{children}</PortalShell>;
}
